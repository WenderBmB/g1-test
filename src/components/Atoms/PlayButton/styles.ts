import styled from 'styled-components'

export const Container = styled.button`
  width: 48px;
  height: 48px;
  border: 0;
  border-radius: 50%;
  background-color: #ffffff;

  &:hover {
    cursor: pointer;
  }
`;
